# Scalene

Works only with Python.
Open a python file and run:

```bash
pip install scalene
scalene run file_name.py
scalene view
```

For coverage:

```bash
pip install coverage
coverage report -m
```

# AST Tree

just copy paste your code and change the language from the menu

[AST tree visualizer](https://astexplorer.net/)

# SonarQube

1. Log in to the cloud
2. Add github organization
3. Add a repository
4. On the `Measures` tab it will show the metrics
