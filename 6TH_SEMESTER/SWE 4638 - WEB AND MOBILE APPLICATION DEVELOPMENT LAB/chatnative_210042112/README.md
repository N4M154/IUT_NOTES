# Real Time Chatting Application - React Native with Firestore

1. In the root to install dependencies:

```
npm install
```

2. In the root:

```
npx expo start
```

3. Create Firebase config credentials in the firebase console
4. scan the QR code in expo go
5. sign up/ log in
